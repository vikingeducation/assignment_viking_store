<h1>All Users</h1>

<p><%= link_to '+ New User', new_user_registration_path %></p>

<ul>
  <% @users.each do |user| %>
    <li><%= user.name %> (<%= user.email %>) <%= has_orders(user) %> | <%= link_to 'show', user %></li>
  <% end %>
</ul>
