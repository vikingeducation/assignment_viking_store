<h3 class="text-center">Admin Actions for <%= @user.name %></h3>

<div class="row">
  <div class="col-sm-8 col-sm-offset-2">
    <p>
      <%= link_to 'Create Order', new_admin_user_order_path(@user), class: 'btn btn-primary btn-block' %>
    </p>
    <p>
      <%= link_to 'Create Address', new_admin_user_address_path(@user), class: 'btn btn-primary btn-block' %>
    </p>
  </div>
</div>
